window_set_size(640,480);
surface_resize(application_surface, 640, 480);

//Make sure interpolulating is turned off when upscaling... 
//It's turned off from global game settings, under the windows tab.
//On the left is a graphics tab, and uncheck it.

